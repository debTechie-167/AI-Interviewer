GEMINI_API_KEY=your_gemini_api_key
# Set to 1 to run the complete UI/API flow using deterministic local fallbacks.
AI_INTERVIEW_DEMO_MODE=0

# Optional: Qdrant Cloud for vector search (defaults to in-memory/keyword fallback on Vercel)
# QDRANT_URL=
# QDRANT_API_KEY=
